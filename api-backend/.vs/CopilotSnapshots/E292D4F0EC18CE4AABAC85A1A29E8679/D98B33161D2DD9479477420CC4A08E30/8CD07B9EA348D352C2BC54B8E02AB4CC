using Microsoft.EntityFrameworkCore;

namespace api_backend.DbContexts
{
    public class AppDbContext : TutorCenterDbContext
    {
        public AppDbContext()
        {
        }

        public AppDbContext(DbContextOptions<AppDbContext> options)
            : base(options)
        {
        }

        // Bạn có thể thêm các customization riêng ở đây nếu cần
        // Ví dụ: override OnModelCreating để thêm cấu hình custom
        
        // protected override void OnModelCreating(ModelBuilder modelBuilder)
        // {
        //     base.OnModelCreating(modelBuilder);
        //     
        //     // Thêm các cấu hình custom riêng cho AppDbContext
        // }
    }
}